spring:
  application:
    name: mapper
  cloud:
    config:
      profile: dev
      label: master
      uri: http://localhost:8888
#      discovery:
#        enabled: true
#        service-id: cloud-config

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/